@model IEnumerable<RegisterModel>

<div class="card text-primary">
    <div class="card-header text-center">
        <h5>Lista de Productos</h5>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <a asp-action="Create" class="btn btn-outline-primary btn-sm">Crear Nuevo</a>
                        <hr />
                        <h5 class="panel-title">Productos</h5>
                    </div>
                    <div class="panel-body">
                        <table class="table table-hover table-responsive table-striped" id="MyTable">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.ID_Register)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Nom_Company)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Nom_Contact)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Email)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Num_Tel)
                                    </th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.ID_Register)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Nom_Company)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Nom_Contact)
                                        </td>

                                        <td>
                                            @Html.DisplayFor(modelItem => item.Email)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Num_Tel)
                                        </td>

                                        <td>
                                            <a asp-action="Edit" asp-route-id="@item.ID_Register" class="btn btn-outline-warning btn-sm">Editar</a>
                                            <a asp-action="Details" asp-route-id="@item.ID_Register" class="btn btn-outline-info btn-sm">Detalles</a>
                                            <a asp-action="Delete" asp-route-id="@item." class="btn btn-outline-danger btn-sm">Eliminar</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="card-footer text-muted text-center">
        Crud Productos
    </div>
</div>